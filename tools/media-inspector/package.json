{
  "name": "estrelalua-media-inspector",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-vendor.mjs",
    "lint": "npm run build && eslint dist scripts tests",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "exifr": "7.1.3",
    "maplibre-gl": "6.7.0",
    "mediainfo.js": "0.3.7"
  },
  "devDependencies": {
    "eslint": "^10.9.1",
    "typescript": "7.0.2"
  }
}
